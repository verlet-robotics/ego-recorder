[package]
name = "egorec_reader"
version = "0.1.0"
edition.workspace = true

[lib]
name = "egorec_reader"
crate-type = ["cdylib"]

[dependencies]
egorec = { path = "../egorec" }
pyo3 = { version = "0.22", features = ["abi3-py310", "extension-module"] }
numpy = "0.22"
